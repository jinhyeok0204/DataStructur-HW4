import heapq

"""
    heapq 모듈은 이진 트리 기반의 최소 힙(min heap) 자료구조를 제공합니다.
    min heap을 사용하면 원소들이 항상 정렬된 상태로 추가되고 삭제된다. 
    heapq 모듈은 파이썬의 리스트를 마치 최소 힙처럼 다룰 수 있도록 도와준다.
    빈 리스트를 생성해놓은 다음 heapq 모듈의 함수를 호출할 때 마다 이 리스트를 인자로 넘겨야한다.
    파이썬에서는 리스트가 그냥 최소 힙이다.
"""